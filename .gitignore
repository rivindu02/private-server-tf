# Local .terraform directories
**/.terraform/*

# .tfstate files - IMPORTANT: Never commit state files!
*.tfstate
*.tfstate.*
terraform.tfstate
terraform.tfstate.backup

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Keep lock file for consistent provider versions (recommended)
# Uncomment the line below if you don't want to track it
# .terraform.lock.hcl

# Ansible
ansible/*.retry
ansible/inventory.ini
*.retry

# SSH keys
*.pem
*.key

# OS files
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# Backup files
*.bak
*.backup

# Environment files
.env
.env.*
!.env.example
